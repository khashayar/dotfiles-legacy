# Make it a login shell...
#defshell -bash
# ...or
shell -$SHELL

# Disable the startup message
startup_message off

# Set a large scrollback buffer
defscrollback 32000

# Scroll back with mouse
termcapinfo xterm* ti@:te@

# Always start `screen` with UTF-8 enabled (`screen -U`)
defutf8 on
